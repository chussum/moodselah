type AddPostResponse {
  success: Boolean!
  error: String
  post: Post
}

type Mutation {
  AddPost(
    content: String
    files: [Upload!]!
    wifi: Int!
    childChair: Int!
    study: Int!
    address: String
    lat: Float
    lng: Float
  ): AddPostResponse!
}
