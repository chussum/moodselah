type User {
  id: Int!
  email: String
  nick: String!
  phoneNumber: String
  profilePhoto: String
  fbId: String
  places: [Place]
  posts: [Post]
  megazines: [Megazine]
  followingCount: Int!
  followerCount: Int!
  verifiedEmail: Boolean!
  verifiedPhoneNumber: Boolean!
  createdAt: String!
  updatedAt: String
}
